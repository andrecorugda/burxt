a `const` goes at the top level, not inside a block: it is a name for the whole program, and it is in scope inside every function. Move it above, or use `let` — which is already immutable without `mutable`.
