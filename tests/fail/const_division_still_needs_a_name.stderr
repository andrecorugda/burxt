`/` on two Ints would have to round, and one operator cannot say which way: -7 divided by 2 is -3 rounding toward zero and -4 rounding down. Say which you mean — `divide_floor(a, b)`, `divide_toward_zero(a, b)`, or `remainder(a, b)` for the remainder.
