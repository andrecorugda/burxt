// `function (self)` borrows its type from an `implement` header. At the top level there is no
// header, so nothing says which type it is — refused with both ways to fix it.
record Book { cost: Int }
function (self) price() -> Int {
    return self.cost;
}
print(1);
