record LineItem { price: Decimal<2>, qty: Int }
function (self: LineItem) total() -> Decimal<2> { return self.price; }
function (self: LineItem) total() -> Decimal<2> { return self.price; }
print(1);
