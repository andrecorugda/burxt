// A bound is `Ordered`, `Equatable`, or an interface this program declares. Anything else is
// a name that means nothing.
function f<T: Nope>(x: T) -> T {
    return x;
}
region r {
    print(f(1));
}
