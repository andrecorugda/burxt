// Returning an array needs whole-array binding at the call site, which
// arrives with collections. Array PARAMETERS work today.
fn make() -> [Int; 3] {
    let a: [Int; 3] = [1, 2, 3];
    return a;
}
print(1);
