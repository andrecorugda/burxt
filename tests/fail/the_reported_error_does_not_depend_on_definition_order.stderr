`pure function fill` cannot take `mutable out: [Node]`: `pure` means the answer depends on the arguments and nothing else, which is the same thing as changing nothing — and `mutable` says this call changes what the caller passed. Drop one of the two. (It matters more than it looks: a contract clause may call a `pure` function, so this would let a precondition rewrite the data it is checking.)
