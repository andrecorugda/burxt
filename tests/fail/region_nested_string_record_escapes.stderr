`got` was declared outside `region r`, so it cannot be assigned a value built inside it — the bytes are released at the closing brace and `got` would read whatever the region hands out next. Declare `got` inside the region, or build the value outside it.
