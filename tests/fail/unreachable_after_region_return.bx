// A region body that returns on every path IS a return, so anything after the
// block is unreachable rather than required.
fn f() -> Int {
    region r {
        return 1;
    }
    return 0;
}
print(f());
