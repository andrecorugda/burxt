// Comparing needs an order, which an unbounded parameter does not have. The signature
// is where a generic says what its parameter can do.
fn bigger<T>(a: T, b: T) -> Bool {
    return a > b;
}
region r {
    print(bigger(2, 1));
}
