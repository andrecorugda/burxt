`h` was declared outside `region r`, so its field cannot be assigned a value built inside it — the bytes are released at the closing brace and `h` would read whatever the region hands out next. Declare `h` inside the region, or build the value outside it.
