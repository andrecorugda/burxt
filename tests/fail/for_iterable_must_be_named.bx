// The loop reads the iterable once per element, so a call would pay its cost on every
// pass. Refused with the fix rather than silently made quadratic.
function sizes() -> Int { return 3; }
region r {
    for x in sizes() {
        print(x);
    }
}
