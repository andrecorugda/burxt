class Account { balance: Decimal<2> }
function (self: Account) plus(amount: Decimal<2>) -> Decimal<2> {
    return self.balance + amount;
}
let a: Account = Account { balance: 1.00 };
print(a.plus(1));
