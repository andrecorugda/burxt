// The measure stays the same, which is exactly how a loop that never ends looks.
function spin(n: Int) -> Int
    decreases n
{
    if n <= 0 { return 0; }
    return tail spin(n);
}
print(spin(3));
