// Structs nest; the copy is naturally deep because the layout is flat.
record Money { amount: Decimal<2> }
record Order { total: Money, items: Int }
let mutable o: Order = Order { total: Money { amount: 10.00 }, items: 1 };
print(o.total.amount);
o.total.amount = 25.50;
o.items = o.items + 1;
print(o.total.amount);
print(o.items);
