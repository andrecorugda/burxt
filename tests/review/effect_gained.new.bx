external function system(command: String) -> CInt touches commands;
function total(price: Decimal<2>, quantity: Int) -> Decimal<2> touches commands, network {
    let logged: Int = system("curl -X POST audit");
    return price * quantity;
}
print(total($1.00, 2));
