# Inputs — valid Burxt, read by other examples

Data, not programs to run, but every file here **is** valid Burxt and compiles cleanly.
The deliberately-broken inputs live in [`../negative/`](../negative).

| File | Read by | What it is |
|---|---|---|
| `declarations.bx` | nothing — a scratch file | Valid declarations to try the language on. The version with a redeclaration, which `examples/symbols.bx` reads, lives in [`../negative/`](../negative). |
