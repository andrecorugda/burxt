/target
*.o
*.ll
# Compiled Burxt outputs are bare, extensionless executables written into the
# working directory, so they are easy to commit by accident. Ignore the ones the
# examples and tests produce; add a line when a new .bx program appears.
/bigparse
/boundary_scaled_marshal
/enum_basic
/enum_match_flow
/enum_payloads
/interp_value
/io
/lexer
/mixed_scale_mul
/money
/order
/p
/parser
/read_file
/region_released_on_return
/sample
/tail_call_constant_stack
/to_string_builtin
# transient delivery artifacts, and the marker Windows attaches to downloads
*.zip
*.tar.gz
*:Zone.Identifier
